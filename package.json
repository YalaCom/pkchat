{
  "name": "pinkchat-pwa",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "vapid": "node scripts/generate-vapid.mjs",
    "configure": "node scripts/configure.mjs",
    "check": "node --check app.js && node --check sw.js && node --check scripts/configure.mjs && node --check scripts/generate-vapid.mjs && node --check scripts/serve.mjs"
  }
}
